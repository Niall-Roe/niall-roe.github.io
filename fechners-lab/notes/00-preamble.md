---
preamble: true
---

Fechner's Lab — example notes

Started 7 August 2026. Several pages rather than one: index, average-error,
just-noticeable-differences and results. An entry should say which page it is
about, since the anchor alone will not distinguish them.

Note the unfinished split into src/ from 4 August — build.sh names
src/00_intro.html, which does not exist, so --check cannot run on this folder
until that is resolved.

Entries follow _status/CONVENTIONS.md. With the review server running, select a
passage on the page and click "+ Add example" to write one.
